; Força o uso do registry público do npm.
; Proteção contra ambientes onde a máquina está autenticada em
; um registry privado (corp / CodeArtifact) — sem isso, o lock
; pode ser regenerado com URLs internas e quebrar deploys.
registry=https://registry.npmjs.org/
